# Dependencies for ctoolbox

This repository holds vendored copies of dependencies for Collective Toolbox.

Some of the dependencies aren't pure source code, unfortunately - rust-icu-*-data-* and some Windows API crates have pre-built binaries.

